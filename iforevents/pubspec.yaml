name: iforevents
description: "A Flutter package for event tracking and analytics integration."
version: 0.0.1
homepage: https://github.com/innovafour/iforevents

environment:
  sdk: ^3.8.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  equatable: ^2.0.7
  android_id: ^0.4.0
  dart_ipify: ^1.1.1
  device_info_plus: ^11.5.0
  package_info_plus: ^8.3.1
  dio: ^5.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
